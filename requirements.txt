Flask==2.3.3
gunicorn==20.1.0

easyocr==1.7.1        # slightly older, smaller model footprint
torch==2.1.0+cpu      # CPU-only version, much smaller
torchvision==0.15.2+cpu  # CPU-only version to match torch

numpy==1.25.0         # older, stable, smaller than latest 2.x
pillow==9.5.0         # lightweight version
opencv-python-headless==4.8.1.74  # stable, small
